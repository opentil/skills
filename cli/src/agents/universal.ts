// Universal agents (Codex, OpenCode, etc.) only need SKILL.md + references.
// No additional extras to install.

export function installUniversalExtras(): void {
  // No-op: universal agents don't have extras
}

export function uninstallUniversalExtras(): void {
  // No-op
}
